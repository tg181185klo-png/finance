/** Default months shown in company history table (Reports tab). */
export const REPORT_HISTORY_MONTHS = 12;

/** Max months the reports API accepts for mode=months. */
export const REPORT_HISTORY_MAX_MONTHS = 24;
